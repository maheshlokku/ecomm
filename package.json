{
  "name": "frontend-app",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run sass",
    "start": "npx serve build",
    "sass": "sass scss:css --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  },
  "packageManager": "npm@10.2.0"
}
